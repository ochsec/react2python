{
  "name": "hello",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=development parcel --log-level 4 react_hello.html --out-dir dist/dev",
    "build": "NODE_ENV=production parcel --log-level 4 build react_hello.html --no-source-maps --out-dir dist/prod",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "react": "^16.14.0",
    "react-dom": "^16.14.0"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.5",
    "parcel-plugin-transcrypt": "^1.0.20"
  }
}
